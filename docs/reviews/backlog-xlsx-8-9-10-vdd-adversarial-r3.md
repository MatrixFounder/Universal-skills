# VDD Critique R3: backlog xlsx-8 / xlsx-9 / xlsx-10 (постановка задач)

**Reviewer**: VDD Adversarial (round 3, fresh-context simulation)
**Target**: [docs/office-skills-backlog.md](../office-skills-backlog.md) §xlsx — rows xlsx-8 / xlsx-9 / xlsx-10 **после round-2 фиксов** (R2-H1, R2-H2, R2-M1..M6, R2-L1..L3)
**Date**: 2026-05-12
**Prior rounds**:
- [round 1](backlog-xlsx-8-9-10-vdd-adversarial.md) — 3H + 8M + 3L findings, все applied.
- [round 2](backlog-xlsx-8-9-10-vdd-adversarial-r2.md) — 2H + 6M + 3L findings, все applied.

## 1. Executive Summary

- **Verdict**: **WARNING** — round-2 фиксы корректно закрыли заявленные findings, но **сам R2-H1 fix скопировал не ту функцию** (truncate_utf16 — no-op для Excel-valid sheet-names; реальная санитация — `History` guard на [naming.py:138-139](../../skills/xlsx/scripts/md_tables2xlsx/naming.py#L138)), а R2-M4 fix создал **type-shape regression** в JSON output (mixed array-of-objects + array-of-arrays в одном документе). Plus 4 MED не-self-introduced дыры (ruff toolchain не существует; drift-detection mechanism не переносится с константы на функцию; Phase B стал accepted-debt-by-default; xlsx-9 effort S оптимистичен против baseline). **Convergence approaching, но НЕ достигнута** — round 4 предсказуемо даст ≤ 1 HIGH (likely hallucinated) + ≤ 3 MED (some forced).
- **Confidence**: **High** для R3-H1/R3-H2/R3-M1/R3-M2 (verified против кода). **Medium** для R3-M3/R3-M4 (heuristic / baseline-comparison). **Low** для R3-L1/R3-L2 (boundary findings).
- **Summary**: Полезный шум сокращается round-over-round: R1 имел 3H surface-claims, R2 имел 2H self-introduced, R3 имеет 2H — но **только 0 self-introduced** (R3-H1/R3-H2 — incomplete fixes, не fix-of-fix). LOW-findings начинают выглядеть forced (R3-L1 CSV-hyperlink layer-violation — известный design choice; R3-L2 sanitize-sheet-names semantic — nitpick). **Trajectory**: одна-две ещё итерации до Maximum Viable Refinement.

## 2. Risk Analysis

| Severity | Category | Issue | Impact | Recommendation |
|:---|:---|:---|:---|:---|
| **HIGH** | Wrong Function Copied | **R3-H1: R2-H1 fix копирует `truncate_utf16` — no-op для Excel-valid sheet-names; реальная санитация это `History` guard** | xlsx-3 [naming.py:120-167](../../skills/xlsx/scripts/md_tables2xlsx/naming.py#L120) applies **9 шагов** для sheet-name санитизации (strip markdown, forbidden chars `[]:*?/\\`, whitespace collapse, fallback Table-N, **UTF-16 truncate**, **History guard**, case-insensitive dedup `-2..-99`). Excel **сам enforces**: ≤ 31 UTF-16 codepoints, нет forbidden chars, нет empty, нет duplicates. Из 9 шагов **только step 7 (History guard)** реально срабатывает на Excel-valid sheet-name "History" (Excel разрешает naming!). R2-H1 fix копирует `truncate_utf16` (60-LOC) — функция, которая **never triggers** для Excel-valid input. Реальная функция, которую надо копировать = 3-строчный History guard (`if base.lower() == "history": base = base + "_"`). | (a) Заменить R2-H1 fix wording: "локальная копия **History-guard logic** в xlsx-9 (3 строки) + 2-way drift-detection test на History guard"; OR (b) **drop `--sanitize-sheet-names` опцию entirely** и просто document в honest scope: «sheet name `History` в source xlsx → emitted as raw H2 `## History`; xlsx-3 round-trip renames to `## History_` per [naming.py:138](../../skills/xlsx/scripts/md_tables2xlsx/naming.py#L138) — expected sanitization, не regression». Pick (b) — меньше surface, нет drift risk вообще. |
| **HIGH** | Output Shape Regression | **R3-H2: R2-M4 fix создаёт mixed JSON shape — array-of-objects + array-of-arrays в одном документе** | R2-M4 fix: `headerRowCount=0` → "respect (headerless emit): JSON array-of-arrays `[[v1,v2],...]` вместо array-of-objects". Sheet с tableA (`headerRowCount=1`) + tableB (`headerRowCount=0`) emit'ит **гетерогенный JSON**: `{"Sheet": {"TableA": [{a:1, b:2}, ...], "TableB": [[1,2], [3,4]]}}`. Downstream consumer (TypedDict / pydantic / `jq -r '.[]'` шаблон) **breaks** — uniform iteration невозможна. xlsx-2 round-trip (`json2xlsx`) тоже breaks: спецификация xlsx-2 принимает только array-of-objects forms, array-of-arrays не handled. | Заменить R2-M4 fix output: при `headerRowCount=0` emit synthetic headers `col_1..col_N` (consistent shape) + per-table metadata flag `{"_headers": null, "rows": [...]}` envelope для downstream разбора; OR sentinel-prefix в headers (`__col_1`); OR sidecar JSON `<base>.headers.json` с `{"TableB": null}`. Pick: synthetic `col_N` headers + warning в `summary.warnings` («Table 'TableB' had no headers; emitted synthetic col_1..col_N»). Uniform shape preserved, lossy nature signalled. |
| **MED** | Non-Existent Toolchain | **R3-M1: R2-M2 fix ссылается на `skills/xlsx/scripts/pyproject.toml` и ruff, которых нет в repo** | Verified: `find skills/xlsx/scripts -name pyproject.toml` → empty (только `.venv/lib/.../pandas/pyproject.toml`). `grep ruff requirements.txt` → empty. `grep ruff install.sh` → empty. R2-M2 fix предполагает существующий toolchain, его нет. Spec'аем enforcement через инструмент, который не установлен — Builder выйдет либо (a) добавит ruff как new dep (отдельный scope creep), либо (b) "забудет" enforcement, оставит docstring-only. | Explicit в xlsx-10 task: «**Phase A includes**: создать `skills/xlsx/scripts/pyproject.toml` с `[tool.ruff.lint.flake8-tidy-imports.banned-api]` rule; добавить `ruff>=0.5.0` в `requirements.txt`; добавить `ruff check scripts/` в `install.sh` post-hook; задокументировать в `skills/xlsx/.AGENTS.md §Toolchain`». OR alternative: drop ruff entirely, использовать существующий validate_skill mechanism (extension grep'ом — R2-M2 признал "wrong tool", но конкретная альтернатива не shipped). Pick: **explicit toolchain bring-up в Phase A scope** + явный LOC budget (~30 LOC config + 1 line install.sh). |
| **MED** | Drift Mechanism | **R3-M2: drift-detection "байт-в-байт" на функцию не переносится с pattern для constant** | R2-H1 fix: «mirror xlsx-3 HEADER_FILL/FONT/ALIGN 3-way drift pattern». Verified в [tests/test_md_tables2xlsx.py:865](../../skills/xlsx/scripts/tests/test_md_tables2xlsx.py#L865): pattern == `self.assertEqual(HEADER_FILL_a, HEADER_FILL_b)` — это **equality на immutable константы** (PatternFill — frozen dataclass-like). Функция `truncate_utf16` имеет **identity** (id()), не value — `func_a == func_b` возвращает `False` всегда (different module objects). "Байт-в-байт" реально означает: либо source-code hash (`inspect.getsource()` — brittle к whitespace / comments), либо AST hash (`ast.dump()` — fragile к Python version), либо behavioral fixture battery (run both на ≥ N inputs, compare outputs). R2-H1 fix не специфицирует которое. Hand-wavy → Builder выберет произвольно. | Specify mechanism в R2-H1 fix wording: **behavioral fixture battery** — `tests/test_xlsx9_naming_drift.py` имеет ≥ 20 input cases (border cases: empty, single-char, exactly-31-utf16, 32-utf16, emoji surrogate pair near boundary, History case-variants `history`/`HISTORY`/`History `, mixed-script CJK + ASCII) — `assertEqual(xlsx9_local_fn(case), md_tables2xlsx_fn(case))` для каждого. Source-code / AST hash explicitly rejected (brittle). |
| **MED** | Acceptance Drift | **R3-M3: R2-H2 fix эффективно permits xlsx-10 Phase B "никогда" — primary motivation становится опциональной** | R2-H2 honest-scope wording: «если xlsx-10.B не открыт за 14 дней → duplication автоматически promoted к accepted technical debt с warning в SKILL.md». Сравните с **xlsx-10 motivation paragraph**: «**Мотивация**: xlsx-8 + xlsx-9 делят ≥ 80% reader-логики ... Без общей библиотеки получаем 2× maintenance burden + drift риск». Round-1 motivation: устранить duplication. Round-2 fix: duplication становится accepted permanent debt. **Self-contradiction по сути не self-introduced** (R2-H2 — honest acknowledgement что magical automation не существует), но **task ROI calculus shift**: Phase A теперь делает 80% value (foundation для xlsx-8/9), Phase B = additional 20% (xlsx-7 dedup) с realistic возможностью never-happens. WSJF backlog priority для xlsx-10 should reflect Phase A standalone. | Сплитнуть xlsx-10 в backlog'е на два явных row: **xlsx-10.A** (foundation для xlsx-8/9; effort M→L; Value H; статус ready); **xlsx-10.B** (xlsx-7 refactor consume; effort M; Value M — closes duplication BUT acceptable-debt-если-skipped; статус ready-but-low-priority; Dep xlsx-10.A). Каждый row отдельно scored по WSJF — current single-row hides realistic likelihood Phase B never shipped. |
| **MED** | Effort Baseline | **R3-M4: xlsx-9 effort `M→S после xlsx-10` оптимистичен против xlsx-2/xlsx-3 baseline** | Backlog history: xlsx-2 initial estimate "S→M", actually shipped **M** (9 steps, 1307 LOC, 73 unit + 11 E2E, ~3 iteration cycles). xlsx-3 initial estimate "S→M", actually shipped **M** (10 steps, 1903 LOC, 94 unit + 14 E2E). xlsx-9 includes: HTML emit + GFM emit + hybrid per-table logic + multi-row header `<thead>`-vs-flatten + cell newlines (`<br>` ⇄ `\n`) + hyperlink GFM+HTML branch + formula `data-formula` attr + drift-detection test + `--sanitize-sheet-names` opt-in + 14 E2E proofs. Этого больше чем у xlsx-2 при том же тонком shim claim. Pattern: **everyone underestimates S in this codebase**. | Поднять xlsx-9 effort `M→S` → **`M`** (даже после xlsx-10). Day-plan: emit-only logic = day 1; multi-format hybrid = day 2; drift / sanitize / 14 E2E + cross-cutting parity = day 3. Honest baseline preserves user expectations. |
| **LOW** | Layer Violation | **R3-L1: CSV hyperlink emit как `[text](url)` markdown — layer violation, downstream видит литералы** | xlsx-8 spec: «CSV cells получают `[text](url)` markdown-link-as-text». RFC 4180 CSV — plain text; embedding markdown semantics в CSV cell means `pandas.read_csv(...)` / `csvkit` / Excel reopen → видит literal string `[click](https://...)`, не link. Альтернативы: (i) `=HYPERLINK("url","text")` formula syntax (нативно работает при reopen в Excel; провалится в `pandas` без quote-handling); (ii) two columns `header` + `header_href`; (iii) URL-only (lossy для display text). Choice не defended в backlog. | Honest scope добавить: «CSV hyperlink encoding = markdown `[text](url)` для symmetry с xlsx-9 markdown emission; downstream CSV consumers видят literal markdown — alternative `=HYPERLINK()` formula rejected because formula-syntax-in-CSV breaks `pandas.read_csv` quote-handling; two-column form (`<h>` + `<h>_href`) — possible v2 if user request». Document trade-off explicitly. |
| **LOW** | Semantic Ambiguity | **R3-L2: `--sanitize-sheet-names` semantic direction unclear** | Flag name doesn't disambiguate: "sanitize emitted H2 *before* writing md" (pre-apply xlsx-3 rules in xlsx-9) vs "post-process xlsx-3 outputs to match xlsx-9 raw names" (impossible — xlsx-3 enforces unconditionally). xlsx-9 row says «xlsx-9 может опционально pre-sanitize emitted H2-headings» — direction clear in code, **но не в flag name**. User reading `--help` sees ambiguous toggle. | Rename `--sanitize-sheet-names` → `--xlsx3-compat-sheet-names` (или `--pre-sanitize-sheet-names`). Hints direction: применить xlsx-3 sanitization rules pre-emit, чтобы xlsx-3 round-trip был byte-identical для sheet-names. (Возможно redundant если R3-H1 рекомендация (b) принята — drop entirely.) |

## 3. Hallucination Check

- [x] **Files / lines verified**:
  - [`md_tables2xlsx/naming.py`](../../skills/xlsx/scripts/md_tables2xlsx/naming.py) — 166 LOC total; `_truncate_utf16` @ line 42; `_sanitise_step2/3/4` @ 58/67/72; `SheetNameResolver` class @ 77; History guard @ 138-139.
  - [`tests/test_md_tables2xlsx.py:865`](../../skills/xlsx/scripts/tests/test_md_tables2xlsx.py#L865) — 3-way drift detection on constants (HEADER_FILL/FONT/ALIGN) via `assertEqual`.
  - [`md_tables2xlsx/writer.py:23-25`](../../skills/xlsx/scripts/md_tables2xlsx/writer.py#L23) — HEADER_FILL = PatternFill(...) constant.
- [x] **Toolchain absence verified**:
  - `find skills/xlsx/scripts -name pyproject.toml` → only `.venv/lib/.../pandas/pyproject.toml` (not skill-owned).
  - `grep ruff skills/xlsx/scripts/{requirements.txt,install.sh}` → empty.
- [x] **xlsx-2 / xlsx-3 effort outcomes verified** by reading their backlog rows: both initial "S→M", both shipped M-scale (1307 LOC / 1903 LOC, 73 / 94 unit tests).
- [ ] **R3-M3 ROI calculus** — qualitative argument, not numerically grounded; depends on user's WSJF weights. Honest gradation: convergence-boundary.
- [ ] **R3-L1 CSV hyperlink consumers behaviour** — not empirically tested via `pandas.read_csv` + `=HYPERLINK()` fixture; based on general CSV-format knowledge. Could be hallucination on edge cases.

## 4. Convergence Signal Assessment

**Round 3 findings distribution**:
- **2 HIGH**: оба — **incomplete R2 fixes**, НЕ fix-of-fix. R3-H1 (wrong function copied), R3-H2 (output shape regression from headerless emit). Self-introduced ratio dropping (R2 had 2/2 self-introduced HIGH, R3 has 0/2 — these are surface-level R2 incompleteness, not new R2-side-effects). **Trajectory positive**.
- **4 MED**: R3-M1 (non-existent toolchain — grounded), R3-M2 (drift mechanism — grounded), R3-M3 (motivation drift — honest concession), R3-M4 (effort baseline — grounded).
- **2 LOW**: R3-L1 (CSV hyperlink layer-violation — known trade-off, could be hallucinated if user explicitly accepts), R3-L2 (flag semantic — pure nitpick).

**Round-over-round trajectory**:
- Round 1: 3H surface (round-trip claim, phased duplication, header-rows multi-table) + 8M + 3L
- Round 2: 2H self-introduced (private-import-vs-closed-API, magical automation) + 6M + 3L
- Round 3: 2H incomplete-R2-fixes (wrong function copied, shape regression) + 4M + 2L

**Convergence NOT reached yet** — R3-H1/R3-H2 are real, grounded HIGH findings. **However**, severity-progression suggests **convergence within 1-2 rounds**:
- R3-H1 is "narrow specific" (wrong function name in fix wording), unlike R1-H1 ("entire contract sealed-broken")
- R3-M findings are largely cosmetic spec-hygiene (toolchain bring-up, drift specifics, motivation phrasing)
- R3-L findings already feel forced

**Exit criteria для round 4**: если ≥ 50% findings — (a) reference to behaviors explicitly out-of-scope, (b) propose remedies для already-fixed problems, OR (c) suggest cosmetic re-wording — **Maximum Viable Refinement** declared. Predict round 4: ≤ 1 HIGH (likely hallucinated), ≤ 3 MED (some hallucinated), several LOW (forced).

## 5. Action Items (Builder)

Applied к backlog'у в той же сессии:

1. **R3-H1**: xlsx-9 row — заменить «локальная копия `truncate_utf16` + drift-detection test» на **drop `--sanitize-sheet-names` опцию** + honest-scope note: «sheet name `History` в source → emitted raw H2; xlsx-3 round-trip renames to `History_` per naming.py:138 — expected sanitization». Drift test обнуляется. Если drop не принят — alternative: «локальная копия **History-guard** (3 строки) + drift-detection (behavioral fixture battery, см. R3-M2)».
2. **R3-H2**: xlsx-10 honest scope (e) — `headerRowCount=0` → emit synthetic `col_1..col_N` headers + warning в `summary.warnings` («Table X had no headers; emitted synthetic columns»). НЕ array-of-arrays — uniform JSON shape preserved.
3. **R3-M1**: xlsx-10 row — explicit «**Phase A includes toolchain bring-up**: создать `pyproject.toml` с `[tool.ruff.lint.flake8-tidy-imports.banned-api]`; `ruff>=0.5.0` в `requirements.txt`; `ruff check scripts/` в `install.sh` post-hook; обновить `skills/xlsx/.AGENTS.md`». LOC budget явно (~30).
4. **R3-M2**: R2-H1 fix wording (если drop не принят per R3-H1) — заменить «байт-в-байт две имплементации» на «behavioral fixture battery: `tests/test_xlsx9_naming_drift.py` имеет ≥ 20 input cases (empty, single-char, exactly-31-utf16, 32-utf16, emoji surrogate near boundary, History case-variants, mixed-script) — `assertEqual` на outputs». Reject source-code / AST hash как brittle.
5. **R3-M3**: split xlsx-10 row в backlog'е на **xlsx-10.A** (foundation, Value H, status ready) + **xlsx-10.B** (xlsx-7 refactor, Value M, status ready-but-low-priority, Dep xlsx-10.A). Каждый WSJF-scored отдельно.
6. **R3-M4**: xlsx-9 effort `M→S` → **`M`**; xlsx-2 / xlsx-3 baseline cited явно как cautionary precedent.
7. **R3-L1**: xlsx-8 honest scope — explicit defence CSV hyperlink encoding choice + rejected alternatives (`=HYPERLINK()` formula, two-column form) с reasons.
8. **R3-L2**: rename `--sanitize-sheet-names` → `--xlsx3-compat-sheet-names` (или drop entirely per R3-H1).

Round 4 запускается после применения изменений. Прогноз: **convergence через 1 round** (≥ 50% findings expected hallucinated/forced).
