# VDD Critique R2: backlog xlsx-8 / xlsx-9 / xlsx-10 (postановка задач)

**Reviewer**: VDD Adversarial (round 2, fresh-context simulation)
**Target**: [docs/office-skills-backlog.md](../office-skills-backlog.md) §xlsx — rows xlsx-8 / xlsx-9 / xlsx-10 **после round-1 фиксов**
**Date**: 2026-05-11
**Prior round**: [backlog-xlsx-8-9-10-vdd-adversarial.md](backlog-xlsx-8-9-10-vdd-adversarial.md) — 3H + 8M + 3L findings, все applied.

## 1. Executive Summary

- **Verdict**: **WARNING** — round-1 фиксы закрыли заявленные findings, но **сами ввели две новые проблемы** (R2-H1: self-contradiction в closed-API rule; R2-H2: magical automation в Phase B deadline) + **выявили 4 MED дыры**, которые round 1 пропустил из-за фокуса на surface-level claims. Convergence НЕ достигнута — round 3 потребуется.
- **Confidence**: **High** — все findings grounded в конкретных строках backlog'а; одна self-contradiction verified чтением privacy-prefix convention из L1 fix.
- **Summary**: Это эффект «fix introduces fix-fix» — типичный для adversarial loop. Round-1 H1 fix предложил xlsx-9 опционально импортировать `md_tables2xlsx.naming._truncate_utf16` (private symbol), что нарушает spirit closed-API rule, введённой в том же round'е (L1 fix). Round-1 H2 fix пообещал «автоматически создаётся blocking ticket» без механизма автоматизации. Round 2 фокусируется на этих self-introduced проблемах + 4 MED дырах в heuristic / spec consistency.

## 2. Risk Analysis

| Severity | Category | Issue | Impact | Recommendation |
|:---|:---|:---|:---|:---|
| **HIGH** | Architecture / Self-contradiction | **R2-H1: Round-1 H1 фикс нарушает round-1 L1 фикс (closed-API rule)** | Round-1 H1 fix предлагает xlsx-9 опционально pre-sanitize sheet-names через «переиспользование `md_tables2xlsx.naming._truncate_utf16` (как dep)». Функция [`_truncate_utf16`](../../skills/xlsx/scripts/md_tables2xlsx/naming.py#L42) underscore-prefixed = private symbol. Round-1 L1 fix установил closed-API rule «приватные модули `_`-prefix, public surface — только `__init__.py`». Cross-skill private-symbol import нарушает тот же принцип, что L1 защищает внутри xlsx-10. Self-contradiction. | Два сценария: (a) Promote `_truncate_utf16` → `truncate_utf16` в xlsx-3 public surface (re-export через `md_tables2xlsx/__init__.py`); требует touch xlsx-3, который DONE — open new task xlsx-3.1. (b) Duplicate function в xlsx-9 (с 3-way drift-detection assertion à la xlsx-3 HEADER_FILL/FONT/ALIGN — `_truncate_utf16` byte-identity check в `tests/test_drift.py`). Pick (b) — не трогает DONE xlsx-3, drift-test ловит divergence. Update round-1 H1 fix text. |
| **HIGH** | Magical Automation | **R2-H2: «Автоматически создаётся blocking ticket xlsx-10.C» — нет механизма** | Round-1 H2 fix обещает «**Phase B имеет hard deadline = ship xlsx-9 + 14 дней, иначе автоматически создаётся blocking ticket xlsx-10.C**». В репо нет cron-job'а, нет CI hook'а, нет ticket-tracker auto-creation, нет calendar-bot'а. «Автоматически» это magical thinking — real consequence: никто ticket не создаст, duplication становится permanent. | Заменить «автоматически» на конкретный механизм: (a) Git pre-push hook в `.git/hooks/pre-push` проверяет timestamp xlsx-9 merge commit + comments в backlog «Phase B due {date}» — fail если >14 дней без xlsx-10.B merge; OR (b) CI workflow `.github/workflows/phase-b-deadline.yml` cron-job ежесуточно grep'ает backlog на "Phase B due" date markers и opens GitHub issue при просрочке; OR (c) honest scope — убрать «автоматически», заменить на «ответственность ownership'а xlsx-9 — открыть xlsx-10.C в течение 14 дней; если нет — duplication становится accepted technical debt с warning в README». Pick (c) — least mechanism, max honesty. |
| **MED** | Logic / Contract | **R2-M1: M7 fix incomplete для `--format hybrid --include-formulas` + merge-free таблица** | M7 fix добавил `IncludeFormulasRequiresHTML` envelope для `--format gfm + --include-formulas`. НО `--format hybrid` per-table выбирает: merge-free таблица → pure GFM (по дизайну hybrid mode). Pure GFM не может нести `data-formula` HTML attribute. Значит в `--format hybrid --include-formulas` workflow merge-free table становится ambiguous: emit как GFM (no formula carrier) или принудительно promote к HTML (нарушение «hybrid auto-per-table»)? Spec не указывает. | Spec: при `--include-formulas` AND `--format hybrid` любая table с formula-cell принудительно promoted к HTML-emission (per-table override hybrid auto-rule); document как honest scope «`--include-formulas` принуждает hybrid mode к HTML для tables с formulas; tables без formulas остаются GFM». |
| **MED** | Wrong Tool | **R2-M2: `validate_skill.py` extension для grep'а closed-API import'ов — wrong tool** | Round-1 L1 fix: «`validate_skill.py` extension grep'ает `from xlsx_read._*` в caller'ах → exit 1». Но `validate_skill.py` — Gold Standard SKILL.md validator (CSO compliance), не linter. Violates separation of concerns: SKILL validation ≠ code linting. Реальные tool'ы: `ruff` (configurable banned-imports rule), `flake8-tidy-imports`, или pre-commit hook. | Заменить enforcement mechanism: `ruff` config в `skills/xlsx/scripts/pyproject.toml` `[tool.ruff.lint.flake8-tidy-imports.banned-api]` с pattern `xlsx_read._*` → fail. Run via `bash scripts/install.sh` post-hook OR via скилла-wide CI gate. Update L1 fix wording. |
| **MED** | Spec DRY | **R2-M3: xlsx-9 honest (i) ↔ xlsx-10 honest (e) — duplicate ListObjects scope statement** | xlsx-9 row: «**ListObjects header-rows** — поддерживается только `headerRowCount=1`; `headerRowCount=0` / >1 → fall back к gap-detect для этого региона». xlsx-10 row: «(e) ListObjects `headerRowCount` ≠ 1 → fall back к gap-detect для этого региона». Same behavior, duplicate spec. Если в Phase A design pass это поведение поменяется — обновлять надо в двух местах, drift риск. | xlsx-9 honest (i) → reference: «ListObjects header-rows handling: см. xlsx-10 honest scope (e) — поведение делегировано в `xlsx_read.tables`, xlsx-9 inherits». Single source of truth. |
| **MED** | Heuristic | **R2-M4: ListObjects `headerRowCount=0` → fall-back к gap-detect WRONG** | Excel ListObject с `headerRowCount=0` — валидная конфигурация (table без header, все строки = data). Fall-back к gap-detect misinterpretirует first non-empty row как header → пропадает первая data-row. Должно быть: respect `headerRowCount=0` — emit table без header keys, JSON output `[[v1, v2], [v3, v4], ...]` (array-of-arrays вместо array-of-objects), markdown — emit без `<thead>`/separator row. | Update xlsx-10 honest scope (e): `headerRowCount=0` → respect (emit headerless); `headerRowCount>1` → respect (multi-row header через xlsx-10 headers.py); fall-back к gap-detect только если ListObject **отсутствует**, не если present-but-unusual. Не fall-back to gap on edge configurations. |
| **MED** | Non-Remedy | **R2-M5: Pin-openpyxl-version как remedy для undefined `OverlappingMerges` behavior — non-remedy** | Round-1 M8 fix: «(iii) если undefined behavior — pin openpyxl version в `requirements.txt`». Pinning version замораживает текущее поведение (whatever it is), не исправляет undefined. Если current openpyxl silently produces wrong result для overlapping merges, pinning сохраняет тот же wrong result. Не remedy. | Phase A spike outcome (iii) → «explicit detect-and-raise через `_overlapping_merges_check(ws.merged_cells.ranges)`», независимо от openpyxl behavior. Pinning version — стандартная hygiene (отдельная задача), не fix для overlapping merges. |
| **MED** | Unmeasured Claim | **R2-M6: «performance overhead для deep-freeze (tuple of tuples)» — unmeasured** | Round-1 M3 fix: deep-freeze к `tuple[tuple[Cell, ...], ...]` «отложен к v2 — performance overhead для 10K+ row reads». `tuple([...])` is O(n), same as `list([...])`. Real argument против deep-freeze — ergonomic friction (can't .append, must build-list-then-convert). Не perf. | Заменить justification: «deep-freeze отложен к v2 — ergonomic friction (build-list-then-convert), не perf concern (`tuple()` constructor is O(n), same as list)». Honest scope. |
| **LOW** | Encoding | **R2-L1: U+203A separator может быть transliterated некоторыми markdown renderers / Windows CP1251 console** | Smart-typography renderers (Pandoc с `--smart`, некоторые Hugo themes) могут convert U+203A → `>` или `&rsaquo;`. Windows console default codepage cp1251 — U+203A не в repertoire, replacement character `?`. Honest scope нужен. | Honest scope в xlsx-8/xlsx-9: «separator ` › ` (U+203A) emitted as raw UTF-8; downstream rendering зависит от reader/renderer encoding; для UTF-8-strict workflow — fine, для legacy CP1251 console — `--header-flatten-style array` recommended». |
| **LOW** | Stale | **R2-L2: M4 default `--gap-rows=2` тоже не measured — заменили one guess другим guess'ом** | Round-1 M4 fix: default 1 → 2 с обоснованием «single-empty-row внутри таблицы — частый Excel-паттерн». OPPOSITE pattern (no-empty-rows-in-table, gap-of-1 как clean separator) тоже частый. Ни тот, ни другой default не measured на реальной выборке Excel-моделей. Meta-finding: heuristic guessed both times. | Acknowledge в honest scope xlsx-10: «`gap_rows=2` chosen как default conservatively (favors under-split over over-split); empirical validation deferred к Phase A — если sample-set of real Excel-models покажет sufficient cases of `gap=1` separator within single table, default reaffirmed; otherwise revisit». |
| **LOW** | Stale Default | **R2-L3: xlsx-10 `read_table(region, header_rows=1)` default stale после H3 fix** | xlsx-10 API default `header_rows=1` был для «backward-compat с xlsx-8». После H3 fix xlsx-8 в multi-table mode форсирует `auto`, не передаёт integer. Default of 1 теперь используется только xlsx-9 single-table mode + xlsx-8 `--tables whole` (legacy). Justification стала circular. | Update xlsx-10 API: `header_rows: int\|Literal["auto"] = "auto"` (default auto — детект через headers.py). Callers с explicit 1 (xlsx-8 `--tables whole --header-rows 1`) передают явно. Default-of-auto уменьшает footgun когда caller забывает override на multi-row data. |

## 3. Hallucination Check

- [x] **Files / line numbers**: `md_tables2xlsx/naming.py:42` (`_truncate_utf16` private prefix) verified в round 1; not re-checked here.
- [x] **Privacy-prefix L1 rule**: re-read из backlog round-1 fix — confirmed scope «приватные модули `_`-prefix... validate_skill.py extension grep'ает `from xlsx_read._*`». R2-H1 self-contradiction confirmed.
- [x] **Magical automation R2-H2**: grep'ом проверено — никаких pre-push hooks, GitHub workflows, или cron-jobs для backlog deadline tracking в репо не существует.
- [x] **R2-M5 «pin doesn't fix undefined»**: argument logically sound, не verified против конкретного openpyxl issue tracker — но утверждение «pinning frozen behavior ≠ correctness fix» tautologically true.
- [ ] **R2-L1 encoding edge cases**: НЕ verified эмпирически — based на known limitations of legacy encodings. Could be hallucination on modern systems (Windows 10+ defaults to UTF-8 in many contexts). **Honest gradation**: R2-L1 is at convergence boundary.

## 4. Convergence Signal Assessment

**Round 2 findings distribution:**
- **2 HIGH**: оба self-introduced round-1 фиксами (H1 self-contradiction, H2 magical automation) — это PRO signal: deep issues, не surface-level. Adversarial loop работает как задумано.
- **6 MED**: смесь incomplete fixes (M1), wrong-tool (M2), spec-DRY (M3), heuristic-too-eager (M4), non-remedy (M5), unmeasured-claim (M6) — все grounded.
- **3 LOW**: R2-L1 (encoding) — близко к hallucination boundary; R2-L2 (meta-finding о guess-character defaults) — valid но низкое actionability; R2-L3 (stale default) — real но nitpick.

**Convergence NOT reached yet** — 2 HIGH self-introduced + 6 MED real findings — slop ещё есть. Тем не менее **trajectory positive**: round 1 had 3H surface findings, round 2 has 2H more sophisticated findings + LOW findings starting to feel forced (R2-L1 encoding). Predict round 3 should have ≤ 1 HIGH (likely hallucinated) + ≤ 3 MED (some hallucinated) — exit signal.

**Exit criteria для round 3**: если ≥ 50% findings либо (a) reference несуществующие строки/контракты, либо (b) propose remedies для already-fixed problems, либо (c) raise concerns about behavior explicitly out-of-scope per honest scope — **Maximum Viable Refinement** declared.

## 5. Action Items (Builder)

Applied к backlog'у в той же сессии:

1. **R2-H1**: round-1 H1 fix text — заменить «переиспользование `md_tables2xlsx.naming._truncate_utf16` (как dep)» на «локальная копия `truncate_utf16` в xlsx-9 + 2-way drift-detection test (mirror xlsx-3 HEADER_FILL pattern)» — closed-API rule preserved.
2. **R2-H2**: round-1 H2 fix — заменить «автоматически создаётся» на ownership-based mechanism (honest scope: «xlsx-9 owner создаёт xlsx-10.C ticket в течение 14 дней; иначе duplication promoted к accepted technical debt в README»).
3. **R2-M1**: xlsx-9 — добавить spec для `--format hybrid --include-formulas + merge-free table` → table principle-promoted к HTML-emission.
4. **R2-M2**: round-1 L1 fix — заменить «validate_skill.py extension» на «`ruff` config `[tool.ruff.lint.flake8-tidy-imports.banned-api]`».
5. **R2-M3**: xlsx-9 honest (i) → reference на xlsx-10 honest (e) (single source of truth).
6. **R2-M4**: xlsx-10 honest (e) — respect `headerRowCount=0` (emit headerless) + respect `headerRowCount>1` (multi-row через `headers.py`); fall-back to gap-detect ТОЛЬКО при absent ListObject.
7. **R2-M5**: round-1 M8 fix — убрать pin-openpyxl как remedy для undefined `OverlappingMerges`; remedy = explicit detect-and-raise независимо от openpyxl version.
8. **R2-M6**: round-1 M3 fix — заменить «performance overhead» justification на «ergonomic friction» для deep-freeze postponement.
9. **R2-L1**: xlsx-8/xlsx-9 honest scope — UTF-8 emission caveat для ` › ` separator + `--header-flatten-style array` recommendation для legacy encodings.
10. **R2-L2**: xlsx-10 honest scope — acknowledge `gap_rows=2` default conservatively chosen, empirical re-validation Phase A.
11. **R2-L3**: xlsx-10 API default `header_rows=1` → `"auto"`; callers с explicit 1 передают явно.

Round 3 запускается после применения изменений. Прогноз: convergence через 1 round (≥ 50% hallucinated findings expected).
