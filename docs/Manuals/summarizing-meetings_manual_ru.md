# Summarizing Meetings — Manual

## Overview

**Summarizing Meetings** — мета-скил для генерации структурированных резюме встреч из текстовых транскрибаций. В отличие от обычного промпта, это **параметризуемый конструктор** — он автоматически определяет тип встречи, выбирает подходящий шаблон и генерирует документ с двумя уровнями детализации (пирамидальная документация).

### Ключевые характеристики

| Характеристика | Описание |
|---|---|
| **Тип** | Meta-Skill (Core) |
| **Execution Mode** | `prompt-first` |
| **Tier** | 2 |
| **Входные данные** | Текст транскрибации |
| **Выходные данные** | Markdown-документ с YAML frontmatter |

---

## Quick Start

### Базовое использование

Передайте агенту текст транскрибации и укажите скил:

```
Используй skill summarizing-meetings:
Сгенерируй резюме встречи из следующей транскрибации:

[текст транскрибации]
```

### С явным типом встречи

```
Используй skill summarizing-meetings --type standup:
Сгенерируй резюме стендапа:

[текст транскрибации]
```

### С указанием пути для сохранения

```
Используй skill summarizing-meetings:
Сгенерируй резюме в файл docs/meetings/2026-02-26-planning.md:

[текст транскрибации]
```

---

## Pipeline обработки

Скил работает в 6 шагов:

```
1. PRE-FLIGHT ──► 2. DETECT ──► 3. SELECT ──► 4. GENERATE ──► 5. VERIFY ──► 6. OUTPUT
   Валидация       Тип           Шаблон       Генерация       Self-Check     Сохранение
   входных         встречи       из assets/    по промпту      8 пунктов      в файл
   данных                                      из references/
```

### Шаг 1: PRE-FLIGHT CHECKS

Перед генерацией агент проверяет:

| Проверка | Действие при провале |
|---------|---------------------|
| Непустой вход | ❌ STOP: ошибка |
| Длина < context window | ⚠️ Chunking (50K блоки с 2K overlap) |
| Определение языка | Указывается в frontmatter |
| Качество ASR | ⚠️ WARN при > 30% мусорных токенов |
| Наличие имён участников | Используется "Участник N" если не извлекается |

### Шаг 2: Автоопределение типа встречи

Агент сканирует первые 20% транскрибации на наличие сигнальных слов:

| Тип | Примеры сигналов |
|-----|-----------------|
| **standup** | "вчера", "сегодня", "блокеры", короткие реплики |
| **retrospective** | "ретро", "что прошло хорошо", "что улучшить" |
| **discovery** | "brainstorm", "идея", "а что если", "варианты" |
| **default** | Нет явных сигналов или неуверенность |

> Полные правила: `references/meeting_type_detection.md`

### Шаг 3: Выбор шаблона

| Тип | Шаблон | Особенности |
|-----|--------|-------------|
| `default` | `assets/template_default.md` | Полная пирамида + все секции |
| `standup` | `assets/template_standup.md` | Done / Doing / Blocked на участника |
| `retrospective` | `assets/template_retrospective.md` | 👍 / 👎 / 🔧 + action items |
| `discovery` | расширенный `default` | Акцент на альтернативы и trade-offs |

---

## Шаблоны

### Default (Основной)

Двухуровневая пирамида:

- **Уровень 1**: TL;DR (3–5 предложений) + таблицы решений и action items
- **Уровень 2**: Логические разделы, каждый содержит:
  - `> Кратко:` — мини-summary для скана
  - `#### Обсуждение` — кто что сказал, аргументы
  - `#### Инсайты` — неочевидные мысли (💡)
  - `#### Решения` — по разделу (✅)

### Standup

Оптимизирован для коротких daily/weekly встреч:
- Таблица **Done / Doing / Blocked** на каждого участника
- Сводка блокеров
- Минимум текста, максимум структуры

### Retrospective

Три классические секции:
- 👍 **Что прошло хорошо** (таблица)
- 👎 **Что пошло не так** (таблица с корневыми причинами)
- 🔧 **Что улучшить** (action items с приоритетами)
- Плюс детальное обсуждение по темам

---

## Obsidian-интеграция

### YAML Frontmatter

Каждое резюме содержит YAML-метаданные, совместимые с Obsidian и Dataview:

```yaml
type: meeting-summary
title: "..."
date: 2026-02-26
meeting_type: default
participants: [...]
duration: "01:00"
languages: [ru]
tags: [meeting, planning, engineering]
related: ["[[Sprint 42 Retro]]", "[[Q2 OKR]]"]
```

### Таксономия тегов

Все теги — из фиксированной таксономии (`references/tag_taxonomy.md`) для consistent графовой навигации:

- **Тип встречи**: `meeting`, `standup`, `retrospective`, `discovery`, `planning` и др.
- **Домены**: `product`, `engineering`, `design`, `data`, `infrastructure` и др.
- **Проекты**: `project/{{name}}`
- **Urgency**: `urgent`, `blocker`, `follow-up`

### Wiki-links

Агент автоматически добавляет `[[wiki-links]]` в `related:` если в транскрибации упоминаются:
- Другие встречи
- Документы
- Проекты
- Системы

---

## Маркеры и конвенции

| Маркер | Значение |
|--------|---------|
| 💡 | Инсайт — неочевидная мысль |
| ✅ | Решение принято |
| 🔲 | Action item — открыт |
| ⚠️ UNKNOWN | Данные не удалось извлечь из транскрибации |
| [НЕРАЗБОРЧИВО] | ASR не распознал фрагмент |
| 🔴 / 🟡 / 🟢 | Уровни приоритета |

---

## Обработка длинных транскрибаций

Для встреч > 1 часа (100K+ символов) скил использует **chunking-стратегию**:

1. Разбивает текст на блоки ~50K символов с overlap 2K
2. Обрабатывает каждый блок по отдельности
3. Мержит результаты: объединяет разделы, дедуплицирует решения и action items
4. Генерирует единый TL;DR по всем блокам

---

## Верификация качества

После генерации агент обязан выполнить **Self-Check**:

```
□ Каждое решение попало в таблицу "Ключевые решения"
□ Каждый action item имеет ответственного
□ Каждый раздел содержит > Кратко: + Обсуждение + Инсайты
□ TL;DR самодостаточен
□ Все цифры/имена/даты сохранены
□ Tags из tag_taxonomy.md
□ Полей ⚠️ UNKNOWN не более 3
□ [[wiki-links]] корректны
```

Затем — **Verification Loop**: повторный проход по транскрибации для поиска пропущенных решений/инсайтов.

---

## Примеры

### Входные данные

См. `examples/example_input_transcript.md` — реалистичная транскрибация Q2 planning meetingс 4 участниками, ~12 минут.

### Ожидаемый результат

См. `examples/example_output_summary.md` — полное резюме по default-шаблону с 4 разделами, таблицами решений, action items, инсайтами и метаданными для агентов.

---

## Файловая структура скила

```
summarizing-meetings/
├── SKILL.md                              # Мета-инструкции агенту
├── assets/
│   ├── template_default.md               # Полный дефолтный шаблон
│   ├── template_standup.md               # Standup-шаблон
│   └── template_retrospective.md         # Retro-шаблон
├── references/
│   ├── generation_prompt.md              # Системный промпт
│   ├── tag_taxonomy.md                   # Таксономия тегов
│   └── meeting_type_detection.md         # Правила autodetect
└── examples/
    ├── example_input_transcript.md       # Пример входа
    └── example_output_summary.md         # Пример выхода
```
