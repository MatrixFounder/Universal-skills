# heal-issues run report — 2026-07-13 11:55, PDF-4

**Mode:** manual (Stage 0 pilot run #1) · **Branch:** `fix/pdf-4-pdf-ocr-vdd-multi-deferred-lows` · **Iterations:** 1/3

## Selection (Phase 0)
| candidate | opened | severity | attempts | verdict |
|---|---|---|---|---|
| **PDF-4** | 2026-06-03 | LOW | 0 | **selected** (oldest) |
| DOCX-MERMAID-EXECSYNC | 2026-06-05 | LOW | 0 | next |
| XLSX-PREVIEW-PNG-ASSERT | 2026-06-05 | LOW | 0 | next |

Preconditions: run-lock free · tree clean · on `main` · config parsed (K=1). All three pilots
carry verified fenced repros as of `f3094a0` (PDF-4 `repro-blocked` cleared with user approval).

## Reproduce (Phase 1) — red before
- L1 stale sidecar: `test ! -e $T/side.txt` → **exit 1** (sidecar survived a simulated mid-OCR crash)
- L2 misclassification: `! grep -q LanguagePackMissing err.json` → **exit 1** (broken tesseract reported as missing lang packs)

## Fix (Phase 2, iteration 1)
- `skills/pdf/scripts/pdf_ocr.py`: L1 — sidecar routed through mkstemp `.partial.txt` in the
  sidecar dir, promoted via `os.replace` after the PDF, scratch added to the `finally` cleanup;
  docstring updated to claim sidecar atomicity. L2 — non-zero `tesseract --list-langs` rc raises
  `OcrEngineUnavailable` with `tesseract_exit`/`stderr` details before any parsing.
- `skills/pdf/scripts/tests/test_pdf_ocr.py`: +3 regression tests (fake-engine sidecar success/
  failure, broken-tesseract rc).

## Gate matrix (all green, iteration 1)
| gate | result |
|---|---|
| repro L1 re-run | exit 0 ✓ |
| repro L2 re-run | exit 0 ✓ |
| unit suite (`.venv`, discover -s tests) | OK (skipped=2) ✓ |
| e2e `test_e2e.sh` | 84 passed, 0 failed ✓ |
| `validate_skill.py skills/pdf` | PASSED ✓ |
| replication | n/a (pdf-only files) ✓ |
| diff guard | 2 files, +95/−6 ≤ 300/10 ✓ |
| protected paths / new deps | none ✓ |

## Review
```sh
git diff main..fix/pdf-4-pdf-ocr-vdd-multi-deferred-lows
# optionally re-run gates, then merge locally; the ledger flip lands with the merge
```
