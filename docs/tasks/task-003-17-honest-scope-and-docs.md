# Task 003.17: Honest-scope locks + SPEC patch (D4) + SKILL.md/.AGENTS.md/examples + validate_skill.py

## Use Case Connection
- **I9.4** (honest-scope regression locks).
- **I9.5** (Docs).
- **R13.a–R13.l** (every honest-scope item locked by a negative test).
- **R14.a–R14.e** (SKILL.md / `.AGENTS.md` / SPEC §5.0 patch / `examples/` / `tests/golden/README.md`).

## Task Goal
Final pre-merge polish. Lock every honest-scope limitation with a negative test (R13.l). Patch SPEC §5.0 from 10 codes to 7 (D4) and move the modern codes to a new §11.x bullet. Update `SKILL.md`, `.AGENTS.md`, `THIRD_PARTY_NOTICES.md`. Commit `examples/check-rules-timesheet.{json,xlsx}`. Run `validate_skill.py` clean.

## Changes Description

### Changes in Existing Files

#### File: `skills/xlsx/references/xlsx-rules-format.md` (SPEC) — **D4 patch, R14.c**

- §Status header: `> **Status**: design spec for backlog item xlsx-7` → `> **Status**: implementation reference (v1 merged YYYY-MM-DD; design history in xlsx-rules-format.md git log).` Update after merge date is set.
- §5.0 (Pre-rule cell triage):
  - Replace the 10-code list `(#REF!, #N/A, #VALUE!, #DIV/0!, #NAME?, #NUM!, #NULL!, #CALC!, #SPILL!, #GETTING_DATA)` with the 7-code list `(#NULL!, #DIV/0!, #VALUE!, #REF!, #NAME?, #NUM!, #N/A)`.
  - Add a parenthetical: `(D4 closure: openpyxl 3.1.5 ERROR_CODES; modern codes #SPILL!, #CALC!, #GETTING_DATA fall through to logical-type=text — see §11 honest scope.)`
- §11.2 Runtime limitations: append a new bullet:
  - `**Modern Excel error codes** — \`#SPILL!\`, \`#CALC!\`, \`#GETTING_DATA\` are NOT recognised by openpyxl <3.2 as \`data_type='e'\`; they round-trip as \`text\`. xlsx-7 v1 does NOT auto-emit \`cell-error\` for them. Workaround: hand-author a \`regex:^#(SPILL|CALC|GETTING_DATA)\` rule on the relevant scope. May be revisited in v2 when openpyxl ships full modern-code support.`

#### File: `skills/xlsx/SKILL.md` — **R14.a**

- §2 Capabilities: add bullet `- **Declarative business-rule validation** — \`xlsx_check_rules.py\` reads a YAML/JSON rules file alongside an .xlsx and emits machine-readable findings; pipes into \`xlsx_add_comment.py\` for cell-comment placement.`
- §4 Script Contract: add `xlsx_check_rules.py` row with full TASK §2.5 flag list (one line, comma-separated short forms).
- §10 Quick Reference: add row per TASK I9.5 AC.
- §12 Resources: add links — `xlsx_check_rules.py`, `examples/check-rules-timesheet.{json,xlsx}`, `references/xlsx-rules-format.md`.

#### File: `skills/xlsx/scripts/.AGENTS.md` — **R14.b**

- Replace the 003.01 placeholder section "## xlsx_check_rules/ — declarative rules-based validation (xlsx-7)" with the final post-merge module map. Format mirrors the existing xlsx_comment/ section. List 12 modules + their F-region mapping + LOC actuals (≤ budget).
- New PyPI deps: `regex>=2024.0`, `python-dateutil>=2.8.0`, `ruamel.yaml>=0.18.0`. `recheck` NOT used (D5 closure).

#### File: `THIRD_PARTY_NOTICES.md` (root) — **CLAUDE.md §3 license hygiene**

- Replace the 003.01 stubs with full attribution sections for `regex`, `python-dateutil`, `ruamel.yaml`. Each section: project name, license name, license-text URL or copy. Same format as existing `lxml` / `defusedxml` entries.

### New Files

- `skills/xlsx/examples/check-rules-timesheet.json` — the SPEC §10 worked example, ready to run. Identical to the rules-file body in SPEC §10.
- `skills/xlsx/examples/check-rules-timesheet.xlsx` — small (~10 KB) timesheet workbook matching the rules file. Generated by `_generate.py` from a manifest committed alongside.

### Changes in `skills/xlsx/scripts/tests/test_xlsx_check_rules.py` — **R13 honest-scope locks**

Add one negative-test class per honest-scope item:

- `TestHonestScopeNoVlookup` — rule with `vlookup`-style reference fails parse; SPEC §11.1 bullet.
- `TestHonestScopeNoDatetimeArith` — `cell:D1 - cell:D2` (both dates) → finding `rule-eval-error`; not crash. (Already in 003.11; restated here for explicit lock.)
- `TestHonestScopeNoAutoFix` — assert that `--output` without `--remark-column` writes a copy unchanged (no auto-fix).
- `TestHonestScopeNoPythonPlugins` — assert `eval`/`exec`/`compile`/`__import__` not present in xlsx_check_rules package source (CI grep).
- `TestHonestScopeNoExcelDataValidations` — workbook with native `<dataValidations>` rules: xlsx-7 ignores them; documented.
- `TestHonestScopeNoMessageLocalisation` — `message` field is single string; multi-language requires running CLI twice.
- `TestHonestScopeNoMultiRowHeaders` — fixture #5 → `MergedHeaderUnsupported` (already in 003.08; restated).
- `TestHonestScopeNoTransposedLayout` — fixture #6 → documented honest-scope error.
- `TestHonestScopeNoMultiAreaName` — multi-area defined name → `RulesParseError(MultiAreaName)` (already in 003.08; restated).
- `TestHonestScopeDecimalPrecisionDocumented` — `Decimal("3.141592653589793238462643383")` round-trips through `float(d)` with documented precision loss; assert behaviour matches docs.
- `TestHonestScopeStaleCacheRequiresRecalc` — fixture #14: warning emitted; spurious findings are user's responsibility.
- `TestHonestScopeModernErrorCodesAsText` (D4) — restated for completeness; fixture #10b.

### Changes in `skills/xlsx/scripts/tests/golden/README.md` — **R14.e**

- "Agent-output-only" warning at the top of the file (mirrors xlsx-6 m4/R9.d).
- Per-fixture provenance table: name, source manifest, SPEC §-anchor.

### Changes in `skills/xlsx/scripts/tests/test_e2e.sh`

- Final cleanup: every fixture exercised; xlsx-7 block matches xlsx_add_comment block in completeness.

## Test Cases
- Unit: ~ 12 honest-scope negative tests + the existing R13.l locks; all pass.
- Battery: 42 fixtures + perf (gated) + 10 canary saboteurs all green.

## Acceptance Criteria
- [ ] SPEC §5.0 patched (10 → 7 codes); §11.2 has the modern-codes bullet.
- [ ] SPEC §Status header updated to "implementation reference".
- [ ] `SKILL.md` §2/§4/§10/§12 updated.
- [ ] `.AGENTS.md` xlsx_check_rules section finalised with module-LOC table.
- [ ] `THIRD_PARTY_NOTICES.md` has full attributions for `regex`, `python-dateutil`, `ruamel.yaml`.
- [ ] `examples/check-rules-timesheet.{json,xlsx}` committed.
- [ ] `tests/golden/README.md` finalised.
- [ ] All R13.a–R13.l honest-scope locks pass.
- [ ] `python3 .claude/skills/skill-creator/scripts/validate_skill.py skills/xlsx` exits 0.
- [ ] `validate_skill.py` exits 0 for the other 3 office skills (`docx`, `pptx`, `pdf`) — sanity check that xlsx-7 did NOT accidentally touch shared `office/` / `_soffice.py` / `_errors.py` / `preview.py` / `office_passwd.py` (CLAUDE.md §2 boundary).
- [ ] `diff -qr skills/docx/scripts/office skills/xlsx/scripts/office` empty (CLAUDE.md §2 byte-identity gate).
- [ ] `diff -qr skills/docx/scripts/office skills/pptx/scripts/office` empty.
- [ ] xlsx-6 unit + E2E suites pass unchanged.

## Notes
- The CLAUDE.md §2 byte-identity gate is the **final regression sanity check**. If it fails, something silently leaked into `office/` or one of the cross-skill scripts; revert and investigate.
- For the SPEC patch: do NOT update the SPEC's git-history-frozen sections (§13 fixture list, §1 purpose). Only §5.0, §11.2 (new bullet), and the §Status header.
- For `THIRD_PARTY_NOTICES.md` license URLs: `regex` is Apache-2.0; `python-dateutil` is Apache-2.0 + BSD-3; `ruamel.yaml` is MIT. Verify each from PyPI before committing.
- This task is the final commit of the chain. After it lands: backlog row `xlsx-7` flips to `✅ DONE` (mirrors `xlsx-6 ✅ DONE` in `docs/office-skills-backlog.md` line 191).
