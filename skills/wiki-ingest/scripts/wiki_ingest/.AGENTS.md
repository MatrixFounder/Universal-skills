# wiki_ingest internal package

Internal package for the wiki-ingest skill. Realised by
[TASK 015 — modular refactor](../../../../docs/tasks/task-015-wiki-ingest-modular-refactor.md)
(merged 2026-05-26). The 11 CLI subcommands live in `commands/*.py`,
the cross-cutting helpers in five `_*.py` modules. `wiki_ops.py` (69
LoC, sibling of this package) is now a thin argparse shim that
iterates `_COMMAND_MODULES` and calls each `register()`.

**TASK 016 — cross-course promotion / demotion (two-tier vault)** — all
11 beads shipped (016.00..016.10). Highlights:

- `_vault.py` gains `find_vault_root` / `discover_courses` /
  `_peek_schema_version` for two-tier discovery (016.00). Symlink-skip
  on the un-resolved ancestor chain + cross-fs guard.
- `_page_merge.py` (NEW F2 module, 016.01) hosts the four additive-merge
  primitives extracted from `commands/upsert_page.py`. Both
  `upsert_page.py` and `promote.py` consume them without violating the
  import-graph invariant.
- `_frontmatter.py` `_splice_frontmatter_fields` now handles
  `list[dict]` (`promoted_from:`) + None-value field removal (016.02).
- `commands/init.py --root` scaffolds the vault-root layer (016.03).
- `commands/lint.py` two-tier mode (mode-detected via schema peek)
  adds four new finding categories: `cross_course_duplicate`,
  `invariant_violation` (HARD failure / non-zero exit),
  `root_footnote_format_warning`, plus dangling-link cross-layer
  refinement. `--limit N` flag for finding-count cap (016.04).
- `commands/promote.py` (NEW, 016.05+06) — dry-run default per Q-2;
  `--apply` does the union frontmatter + additive body merge +
  literal-line-diff contradiction detection (Q-10) + vault-relative
  footnote rewrite (A-M-2) + index/log updates + course-copy deletion.
- `commands/demote.py` (NEW, 016.07) — cross-course citation refusal
  (R5.2) before any write; footnote rewrite to short form;
  `promoted_from` removal via splice.
- `commands/reindex.py` (016.08) — schema-peek mode detection (M-4);
  root mode rebuilds root index.md without `## Sources`; course mode
  adds `## Shared concepts/entities referenced` sections; `--cascade`
  reindexes every course.
- `commands/upsert_page.py` (016.09) — root-first lookup; vault-relative
  footnote on root pages; no auto-promotion (R8.5).

**Layered DAG honoured throughout**: every command imports `_safety`,
`_markdown`, `_frontmatter`, `_vault`, `_page_merge` — never another
command. `tests/test_architecture.py` enforces this via stdlib `ast`.

**LoC budgets vs architecture §3.2**:
- `_vault.py`: 296 / ≤250 (+18%) — symlink pre-flight + cross-fs guard.
- `_page_merge.py`: 95 / ≤150 (within).
- `_frontmatter.py`: 368 / ≤350 (+5%) — list-of-dicts + removal path.
- `lint.py`: 603 / ≤450 (+34%) — two-tier extensions span 4 new categories.
- `promote.py`: 596 / ≤400 (+49%) — full apply path + 9 helpers.
- `demote.py`: 304 / ≤300 (within).
- `reindex.py`: 393 / ≤350 (+12%) — root-mode + shared-referenced.
- `upsert_page.py`: 213 / ≤250 (within).

Overruns are functional content (no dead code or speculative
abstractions). Architect may revisit budgets in a follow-up.

## Layered model — one-way dependency rule

```
F3 commands/*.py + _vault.py + _classify.py   ↑
F2 _markdown.py + _frontmatter.py             ↑
F1 _safety.py                                  (root)
```

- **F1 imports nothing from siblings.**
- **F2 imports only from `_safety` (F1).**
- **F3 imports from F1 / F2 / F3-siblings — but NEVER from another command.**
- **Enforced** by `tests/test_architecture.py` (ast-walk, ~30 LoC of real
  logic). Failing this test = the DAG is broken; fix the import, don't
  relax the test. **Known blind spot**: dynamic imports
  (`importlib.import_module`, `__import__`) are not detected — catch
  those in code review.

## What lives where

| Module           | LoC | Owns                                                                                                  |
|------------------|-----|--------------------------------------------------------------------------------------------------------|
| `_safety.py`     | 339 | atomic I/O (tmp-file + fsync + `os.replace`, tmp cleanup on failure), NFKC `slugify`, `_safe_name` (incl. `[`, `]`, `|`, `^` rejection + slug-collision via `os.scandir`), `_safe_inline`, `_safe_for_json` (control-char strip + len cap), `_check_case_collision`, `_collect_names`, `_LOG_FORBIDDEN_IN_DETAIL`, size constants |
| `_markdown.py`   | 366 | code-fence + inline-construct masking (offset-preserving), section locators with per-header LRU-cached regex, `replace_section_body` with masked= propagation, anchor-aware wiki-link extraction, abbreviation-skipping `_first_sentence`, blockquote-stitching `_existing_lines` |
| `_frontmatter.py`| 333 | line-anchored YAML split (defeats `\n---` substring), `_strip_quotes` (pass-through on mismatched), flow-list parser, structural `_splice_frontmatter_fields`, fast body extractor |
| `_vault.py`      | 296 | TASK 015: layout constants (`DEFAULT_SUBDIRS`, `SUBDIR_TO_*`, `SCHEMA_FILE`/`INDEX_FILE`/`LOG_FILE`, `ASSETS_DIR`), `_walk_pages` (symlink-skipping), `load_vault_pages`, `ensure_schema`, `load_asset`, `tail_log` (ASCII-anchored regex + 64 KiB seek-from-end fast path). TASK 016: `_peek_schema_version`, `_walk_up_for_schema`, `find_vault_root`, `discover_courses` (un-resolved-ancestor symlink-skip + cross-fs guard + sorted output) |
| `_page_merge.py` |  95 | TASK 016: additive-merge primitives extracted from `commands/upsert_page.py`. `upsert_source_row`, `append_fact`, `append_contradiction`, `upsert_footnote`. F2 tier — imports `_markdown` only. Reused by `commands/upsert_page.py` and (future) `commands/promote.py`. |
| `_classify.py`   | 293 | folder-classification: ext/hint tables, `_count_md_structure` (LRU-cached), `_filename_hint_score`, `_pick_primary`, `_detect_grouping`, `_group_files`, `_UNGROUPED_SENTINEL` (process-unique object) |
| `commands/<cmd>.py` | 55-603 | one CLI subcommand each; exposes EXACTLY `register(subparser)` + `execute(args) -> int`. **No command imports another command.** TASK 016 added `promote.py` + `demote.py` and extended `init.py`, `lint.py`, `reindex.py`, `upsert_page.py`. |

## Adding a new command

1. Create `commands/<your_cmd>.py` with `register` + `execute`.
2. Add it to `_COMMAND_MODULES` in `../wiki_ops.py` (2-line change: import + tuple entry).
3. Add `../tests/commands/test_<your_cmd>.py` with ≥1 register test + ≥1 execute happy-path test + ≥1 adversarial test where applicable.

## Where does a new helper go?

| Used by … | Goes in … |
|-----------|-----------|
| Exactly one command, no security/markdown content | Keep it command-local (private `_` prefix) |
| ≥2 commands, atomic I/O / NFKC / safe-name etc.  | `_safety.py` (F1) |
| ≥2 commands, markdown structure                   | `_markdown.py` (F2) |
| ≥2 commands, YAML frontmatter                     | `_frontmatter.py` (F2) |
| ≥2 commands, vault layout / walk / log            | `_vault.py` (F3-helper) |
| `classify-folder` only                             | `_classify.py` (F3-helper) |
| ≥2 commands cross-tier                            | usually means the abstraction is wrong — ask first |

## What NOT to do

- Do NOT import from a `_*.py` module into a sibling on the same tier
  unless the dependency is acyclic (the rule above forbids cycles).
- Do NOT introduce a new helper module without first deciding which tier
  it lives on. If a function is used by ≥2 commands, it likely belongs
  in F1 or F2, not duplicated.
- Do NOT add runtime dependencies. wiki-ingest is pure stdlib.
- Do NOT touch `wiki_ops.py` from inside this package — the shim is the
  external contract; this package is its implementation.
- Do NOT relax `tests/test_architecture.py` to make a forbidden import
  pass; fix the import.
