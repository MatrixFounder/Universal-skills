# SPEC §13.1 fixture #1 — happy path; ok=true; findings=[].
id: 1
name: clean-pass
shape: simple-grid
rules_format: json
xfail: false   # 003.14b orchestrator end-to-end works; promoted from xfail
sheet:
  name: Sheet1
  cells:
    A1: { type: header, value: "Hours" }
    A2: { type: number, value: 8 }
    A3: { type: number, value: 7 }
    A4: { type: number, value: 8.5 }
rules: |
  {
    "version": 1,
    "rules": [
      {
        "id": "hours-positive",
        "scope": "col:Hours",
        "check": "value > 0",
        "severity": "error"
      }
    ]
  }
expected:
  exit_code: 0
  summary:
    errors: 0
    warnings: 0
  required_rule_ids: []
  forbidden_rule_ids: []
