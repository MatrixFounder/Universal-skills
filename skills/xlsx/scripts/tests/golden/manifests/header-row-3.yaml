# SPEC §13.1 fixture #3 — locks `defaults.header_row: 3` override (§4.2).
id: 3
name: header-row-3
shape: simple-grid
rules_format: json
xfail: false   # 003.14b orchestrator end-to-end works; promoted from xfail
sheet:
  name: Sheet1
  cells:
    A1: { type: text,   value: "Report Title" }
    A2: { type: text,   value: "Generated 2026-05-08 by xlsx-7 fixture generator" }
    A3: { type: header, value: "Hours" }      # header_row = 3
    A4: { type: number, value: 8 }
    A5: { type: number, value: 7 }
    A6: { type: number, value: 9 }
rules: |
  {
    "version": 1,
    "defaults": { "header_row": 3 },
    "rules": [
      {
        "id": "hours-positive",
        "scope": "col:Hours",
        "check": "value > 0",
        "severity": "error"
      }
    ]
  }
expected:
  exit_code: 0
  summary:
    errors: 0
  required_rule_ids: []
  forbidden_rule_ids: []
