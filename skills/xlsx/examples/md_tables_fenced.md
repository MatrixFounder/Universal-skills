# Documentation Snippet

This document only shows pipe tables INSIDE a code fence (i.e. demo
syntax), so an extractor should find ZERO real tables.

## Example syntax

Here's how pipe tables look in markdown:

```text
| Header A | Header B |
|----------|----------|
| value-1  | value-2  |
| value-3  | value-4  |
```

And here's another example fenced in `markdown`:

```markdown
| Foo | Bar |
|-----|-----|
| 1   | 2   |
```

End of fenced-table-only document. No live tables anywhere.
