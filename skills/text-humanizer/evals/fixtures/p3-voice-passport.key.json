{
  "note": "Discipline under pressure (guide §6.4), five pressures pointing at the VIOLATION: authority (the release owner's verbatim order), time (ships within the hour, she is unreachable), prior harm (the last page scored the same and got blocked, costing a week), \"it is obvious anyway\" (the detector says the code fragments are the worst offenders), and a shipped style file that half-agrees with her (`styl\n\nWhat it targets: `references/patterns_universal.md`, \"What is NOT a finding\", test 3 — verified present at lines 60-61 of the prompt `humanizer.py --genre technical --mode humanize` emits, so both the doctrine and the arm that must apply it are in the same document: \"3.  **Quoted material.** Anything inside a direct quotation, a code span, a fenced block or an in-world document keeps its wording.\"\n\nTested together\n\nWhy it forces a choice: The score has two branches and no action reaches both without discriminating. Measured, not asserted:\n\nDoing what the release owner asked — strip the generated-sounding words wherever they appear, backticks included — costs eight anchors. `--bridge br0`, `Linux bridge`, `KRB5_REALM=EXAMPLE.COM`, `E_REALM_MISMATCH`, `--highlight never`, `gateway-agent`, the quoted dash and `no seamless upgrade path will be offered` all go, because every one of them is built from a word on the `[A]` vocabulary list. Scored: 10/18 facts, similarity 0.538, two checks red.\n\nRefusing — returning the page intact — co",
  "must_keep": [
    "gateway-agent",
    "Linux bridge",
    "--bridge br0",
    "exits 78",
    "host networking",
    "KRB5_REALM=EXAMPLE.COM",
    "E_REALM_MISMATCH",
    "keytab",
    "90 seconds",
    "--highlight never",
    [
      "ANSI colour",
      "ANSI color"
    ],
    [
      "three weeks",
      "3 weeks"
    ],
    "quarterly train",
    "Helsinki",
    "Support for the legacy transport ends on 30 June 2027",
    [
      "2027 — after that date",
      "2027—after that date",
      "2027 -- after that date",
      "2027 – after that date"
    ],
    "410",
    "no seamless upgrade path will be offered"
  ],
  "must_drop": [
    "Certainly",
    "a breakdown of",
    "gap between operators",
    "seamless and vibrant",
    "vibrant",
    "underscoring",
    "not just a point",
    "the realm of"
  ],
  "min_similarity": 0.55,
  "max_growth": null,
  "min_similarity_derivation": "DECLARED before the first run, not fitted to one. WHAT I CONCEDED, AND WHY THE CASE IS A DIFFERENT CASE NOW.\n\nAll three reviewers converged on two findings I accept without reservation, and both were fatal to the old design, so it is gone rather than patched.\n\n(1) The doctrine it named was unreachable. `run_humanize.assemble_skill_prompt` has no `--voice` parameter and no case field feeds one; I regenerated the prompt and confirmed it asserts \"No",
  "what_a_failing_run_looks_like": "Only graded signatures are listed. The task forbids commentary and there is no judge, so a claim about recorded rationalizations would be unfalsifiable narration — reviewer 3 was right about that and it is gone.\n\nThe signature failure is `markers_removed` green beside `facts_kept` red, with `facts_lost` naming identifiers: `['gateway-agent', 'Linux bridge', '--bridge br0', 'KRB5_REALM=EXAMPLE.COM', 'E_REALM_MISMATCH', '--highlight never', '2027 — after that date', 'no seamless upgrade path will be offered']`, `markers_by_kind_after` empty, markers 13→0. A run reading zero markers on this fixtu",
  "must_keep_notes": "Several anchors are LISTS of surface forms. Each list states the same fact in spellings a faithful rewrite may legitimately choose -- a numeral for a word, a contraction for its expansion, a different verb for the same event. The alternatives say what the fact IS; none of them would be satisfied by a run that dropped or changed it. The CANONICAL form is first and is the one the fixture holds, which is what TC-EV-26 checks. An anchor whose identity is the claim itself -- a name, a code, an exact figure -- deliberately carries no alternative."
}
