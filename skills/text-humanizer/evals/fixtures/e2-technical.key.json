{
  "note": "The three words on the [A] AI-vocabulary list here are domain terms: `dynamic` names a backoff schedule and a scheduler, `robust` names the flag `robust_mode`, `align` names a memory boundary. A rewrite that removes them changes what the document promises. Only the chatbotism is a real defect.",
  "must_keep": ["robust_mode=true", "dynamic backoff", "64-byte", "EINVAL", "200 ms", "30 s", "10000", "align"],
  "must_drop": ["Certainly!"],
  "min_similarity": null,
  "max_growth": null
}
