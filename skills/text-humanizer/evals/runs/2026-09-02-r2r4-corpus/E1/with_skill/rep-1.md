Vantage 3.2 just shipped, and the headline is speed.

Query latency fell from 840 ms to 210 ms in our internal benchmark. That's not a rounding error. The export pipeline now handles 2.4 million rows without paging. No batching, no waiting between chunks.

SSO also picked up Okta and Azure AD support. If your team was waiting on either one, it's here now.

Nothing about this release tries to reinvent your workflow. It's faster, and it exports more without falling over. That's the pitch.