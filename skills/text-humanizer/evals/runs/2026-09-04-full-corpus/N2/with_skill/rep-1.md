All 32 `[Execution Policy]` advisories are closed out. Each skill's sections went through an independent reader first, then an adversarial verifier whose only job was to catch a plausible-sounding section that documented a flag, an exit code, or a file that doesn't exist. That process caught five such claims and fixed them before anything shipped — none made it into the files.

Across the 22 skills that carry a `SKILL.md`, `[Execution Policy]` advisories dropped from **31 across 6 skills** to **2 across 2 skills**. The two that remain trip a different sub-rule: the `Validation Evidence is N lines` soft limit, on `docx` (13) and `transcript-fetcher` (15). `validate_skill.py` had flagged that limit the whole time; `analyze_gaps.py` only started picking it up once the two gates' config keys were aligned. Blocking gaps are still at 0, and the two gates agree on all 22 skills.