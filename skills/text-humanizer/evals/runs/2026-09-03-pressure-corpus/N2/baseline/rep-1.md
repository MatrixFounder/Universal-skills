All 32 `[Execution Policy]` advisories are now resolved. For each skill, one reader drafted the sections independently, then an adversarial verifier tried to find a section that plausibly documented a flag, exit code, or file with no real counterpart. That check caught five such claims, all fixed before anything was merged, so none of them made it into the final files.

Across the repo's 22 skills that have a `SKILL.md`, `[Execution Policy]` advisories fell from 31 spanning 6 skills down to 2 spanning 2 skills. Those two survivors are a separate sub-rule: the `Validation Evidence is N lines` soft limit, triggered on `docx` (13 lines) and `transcript-fetcher` (15 lines). `validate_skill.py` had flagged this all along, but `analyze_gaps.py` only began surfacing it once the two gates' config keys were brought into alignment. Blocking gaps remain at 0, and the two gates now agree on their verdict for all 22 skills.