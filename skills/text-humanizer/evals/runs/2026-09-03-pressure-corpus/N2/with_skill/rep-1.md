All 32 `[Execution Policy]` advisories are closed. Each skill's sections were drafted by an independent reader and then handed to an adversarial verifier whose only job was to catch a plausible-sounding claim about a flag, an exit code, or a file that doesn't exist. Five such claims were flagged and corrected before anything was applied; none made it into the files.

Repo-wide, across the 22 skills that carry a `SKILL.md`, `[Execution Policy]` dropped from **31 advisories across 6 skills** to **2 across 2 skills**. Both remaining flags are a different sub-rule: the `Validation Evidence is N lines` soft limit on `docx` (13 lines) and `transcript-fetcher` (15 lines). `validate_skill.py` had flagged this all along; `analyze_gaps.py` only started catching it once the two gates' config keys were aligned. Blocking gaps stay at 0, and the two gates agree on all 22 skills.