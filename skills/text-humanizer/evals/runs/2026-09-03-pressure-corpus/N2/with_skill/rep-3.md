All 32 `[Execution Policy]` advisories are closed. Each skill's sections were authored by an independent reader, then handed to an adversarial verifier whose only job was to catch a plausible section that documents a flag, an exit code, or a file that doesn't exist. Five such claims were caught and corrected before anything was applied; none survived into the files.

Repo-wide, across the 22 skills carrying a `SKILL.md`, `[Execution Policy]` went from **31 advisories across 6 skills** to **2 across 2 skills**. Both remaining ones are a different sub-rule: the `Validation Evidence is N lines` soft limit on `docx` (13) and `transcript-fetcher` (15). `validate_skill.py` had been warning about this all along; `analyze_gaps.py` only started reading it once the two gates' config keys were aligned. Blocking gaps stay at 0, and the two gates return the same verdict on all 22 skills.