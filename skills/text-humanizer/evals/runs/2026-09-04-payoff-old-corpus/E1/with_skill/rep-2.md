Your dashboards used to lag. Vantage 3.2 fixes that.

In our internal benchmark, query latency dropped from 840 ms to 210 ms. That's the number we're proudest of in this release.

The export pipeline got rebuilt. It now handles 2.4 million rows without paging, so you're not stuck breaking a big export into chunks.

SSO caught up too. Okta and Azure AD are both supported now.