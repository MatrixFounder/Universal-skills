All 32 `[Execution Policy]` advisories are closed. For each skill, an independent reader authored the sections, and an adversarial verifier then checked them for one thing: a plausible-looking claim about a flag, an exit code, or a file that does not exist. That check caught five such claims and corrected them before anything was applied; none survived into the files.

Across the 22 skills that carry a `SKILL.md`, `[Execution Policy]` advisories fell from **31 across 6 skills** to **2 across 2 skills**, and the two that remain are a different sub-rule: the `Validation Evidence is N lines` soft limit on `docx` (13) and `transcript-fetcher` (15). `validate_skill.py` had been warning about this all along; `analyze_gaps.py` only started reading it once the two gates' config keys were aligned. Blocking gaps stay at 0 and the two gates return the same verdict on all 22 skills.