Nobody loves waiting on a query, and Vantage 3.2 is built to shrink that wait.

Query latency dropped from 840 ms to 210 ms in our internal benchmark, and the new export pipeline handles 2.4 million rows without paging. SSO also supports Okta and Azure AD.

Less waiting, more querying. That's 3.2.