Vantage 3.2 is out. Here's what actually changed.

Query latency dropped from 840 ms to 210 ms in our internal benchmark. The new export pipeline handles 2.4 million rows without paging. SSO now supports Okta and Azure AD.

That first number is the one worth sitting with: four times faster, on the same queries your team runs every day.

We built this because waiting on dashboards was killing momentum. Now it doesn't.