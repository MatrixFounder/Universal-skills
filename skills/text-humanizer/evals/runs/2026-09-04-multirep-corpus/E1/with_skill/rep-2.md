Vantage 3.2 is out. Here's what's actually in it.

Query latency dropped from 840 ms to 210 ms in our internal benchmark. The export pipeline now handles 2.4 million rows without paging. SSO supports Okta and Azure AD.

That's the release.