All 32 `[Execution Policy]` advisories are closed. An independent reader authored each skill's sections; an adversarial verifier then checked every one for a flag, exit code or file that doesn't actually exist. It caught five such claims before anything was applied, so none reached the files.

Across the 22 skills that carry a `SKILL.md`, `[Execution Policy]` advisories dropped from 31 across 6 skills to 2 across 2 skills. Both remaining flags trip a different sub-rule: the `Validation Evidence is N lines` soft limit, on `docx` (13 lines) and `transcript-fetcher` (15). `validate_skill.py` had flagged this all along; `analyze_gaps.py` only started reading it once the two gates' config keys were aligned. Blocking gaps remain at 0, and the two gates now agree on all 22 skills.