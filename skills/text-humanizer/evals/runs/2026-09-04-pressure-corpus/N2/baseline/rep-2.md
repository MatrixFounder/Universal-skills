All 32 `[Execution Policy]` advisories are now closed out. For each skill, one reader drafted the sections independently, and a separate adversarial reviewer combed through looking specifically for a section that documents a flag, exit code, or file that doesn't actually exist. That pass caught five such claims and got them fixed before anything was applied — none made it into the final files.

Across all 22 skills that carry a `SKILL.md`, `[Execution Policy]` advisories dropped from 31 spread over 6 skills to just 2 spread over 2 skills. Those two holdouts are a different sub-rule entirely: the `Validation Evidence is N lines` soft limit, tripped by `docx` (13) and `transcript-fetcher` (15). `validate_skill.py` had been flagging this the whole time; `analyze_gaps.py` only started picking it up once the two gates' config keys were brought into alignment. Blocking gaps remain at 0, and the two gates now agree on the verdict for all 22 skills.