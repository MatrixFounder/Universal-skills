All 32 `[Execution Policy]` advisories are now closed out. The approach: an independent reader wrote each skill's sections, then an adversarial verifier went through looking for exactly one thing — a section that plausibly documents a flag, an exit code, or a file that isn't actually there. That process caught five such claims and got them fixed before anything shipped, so none made it into the final files.

Looking at the full repo — 22 skills with a `SKILL.md` — `[Execution Policy]` advisories dropped from 31 across 6 skills down to just 2 across 2 skills. Those two survivors aren't the same issue, though; they're a separate sub-rule, the "Validation Evidence is N lines" soft limit, tripped by `docx` (13 lines) and `transcript-fetcher` (15 lines). `validate_skill.py` had actually been flagging this the whole time — `analyze_gaps.py` only picked it up once the two gates' config keys were brought into alignment. Blocking gaps remain at zero, and the two gates now agree on verdicts across all 22 skills.