# scripts/.AGENTS.md — pdf skill Python script directory

Agent orientation file. Describes each script/module in
`skills/pdf/scripts/`, its purpose, and cross-references to references and
tests. Created during TASK 018 (pdf-4 `pdf_ocr.py`); keep it in sync at the
close of each pdf-N feature chain.

---

## Script inventory

| File | Purpose |
|------|---------|
| `md2pdf.py` | Markdown → PDF via weasyprint + markdown2; fenced mermaid blocks pre-rendered to PNG via `mmdc`; nested PDF outline out of the box. |
| `html2pdf.py` + `html2pdf_lib/` | HTML / MHTML / `.webarchive` → PDF via weasyprint (default) or an opt-in Chrome engine (`--engine chrome`, `install.sh --with-chrome`). 7-module preprocessing pipeline. |
| `pdf_merge.py` | Bookmark-preserving merger via pypdf. |
| `pdf_split.py` | Range / per-page / fixed-chunk splitter. |
| `pdf_watermark.py` | Text/image watermark overlay via reportlab + pypdf; per-mediabox overlay cache; cross-7 same-path guard. |
| `pdf_fill_form.py` | AcroForm inspect/extract/fill/flatten via pypdf; XFA detected + refused (exit 11/12 triage). |
| `pdf_extract.py` | Per-page text + tables → structured JSON **dump** via pdfplumber; **scan detection** (image-only document → exit `10 DocumentScanned`). NOT a Markdown converter. |
| `pdf_ocr.py` | **OCR a scanned PDF → searchable PDF** via `ocrmypdf` (default `eng+rus`). Remediation hop for `pdf_extract.py` exit 10. Soft-optional engine (lazy import; `install.sh --with-ocr`). All hard failures exit 1 with an envelope `type` discriminator (no new exit codes; `10` reserved to `pdf_extract.py`). See `references/ocr.md`. |
| `preview.py` | Universal `INPUT → PNG-grid` renderer (`.pdf` via Poppler; OOXML via LibreOffice + Poppler). **Byte-identical across all four office skills** — edit the docx master, replicate (CLAUDE.md §2). |
| `_errors.py` | `--json-errors` envelope helper (schema `v=1`: `{v, error, code, type?, details?}`). **Byte-identical across the four office skills** (docx is master). Imported read-only by the pdf CLIs. |
| `mermaid-config.json` | Bundled office-friendly mermaid config (Cyrillic-capable font stack). |
| `requirements.txt` | Base Python deps (pypdf, pdfplumber, weasyprint, markdown2, reportlab, Pillow). |
| `requirements-chrome.txt` | Soft-optional Playwright (Chrome engine for html2pdf). `install.sh --with-chrome`. |
| `requirements-ocr.txt` | Soft-optional `ocrmypdf` (for `pdf_ocr.py`). `install.sh --with-ocr`. |
| `install.sh` | venv bootstrap + native-lib / system-tool probes. Flags: `--with-chrome` (Playwright + Chromium), `--with-ocr` (ocrmypdf + probe tesseract/eng/rus/ghostscript). |
| `package.json` | Node deps (mermaid-cli) for md2pdf mermaid rendering. |

## Tests (`tests/`)

- `test_e2e.sh` — end-to-end smoke across the CLIs (md2pdf, merge, split,
  watermark, fill_form, extract, pdf_ocr soft-skip block, outline, visual
  regression).
- `test_pdf_extract.py` + `_pdf_extract_fixtures.py` — pdf_extract unit/E2E.
- `test_pdf_ocr.py` + `_pdf_ocr_fixtures.py` — pdf_ocr unit/E2E. Engine-free
  units (mocked import, explicit installed-set, fake ocrmypdf module) always
  run; the real-OCR composition E2E **soft-skips** unless ocrmypdf + tesseract +
  ghostscript are installed.
- `test_battery.py` — html2pdf regression battery.

## Cross-skill replication (CLAUDE.md §2)

`_errors.py` and `preview.py` are byte-identical across docx/xlsx/pptx/pdf
(docx is master). Everything else here is pdf-only — `pdf_ocr.py` imports
`_errors.py` read-only and triggers **no** replication.

## TASK 018 (pdf-4 `pdf_ocr.py`) — VDD develop-all

`pdf_ocr.py` shipped via beads 018-01 (stub + packaging + fixtures), 018-02
(engine probe + language validator), 018-03 (ocrmypdf runner + exception
mapping), 018-04 (`--password` decrypt-to-temp via pikepdf), 018-05
(`--deskew`/`--rotate-pages`/`--clean` with `osd`/`unpaper` prerequisite probes),
018-06 (this doc + SKILL.md surface + notices + e2e block + validation).
Engine-free + pikepdf-decrypt + knob-prereq test coverage is complete; the
real-OCR composition E2Es soft-skip on hosts where the spawned tesseract cannot
read ocrmypdf's nested temp dir (e.g. a sandboxed environment) — run
`bash tests/test_e2e.sh` in a normal shell to verify them.
