{
  "name": "universal-skills-pdf",
  "version": "0.1.0",
  "private": true,
  "description": "Optional Node CLI deps for the pdf skill: mermaid-cli renders ```mermaid blocks to SVG, and katex renders $…$/$$…$$ math to MathML, before weasyprint takes over.",
  "license": "SEE LICENSE IN ../LICENSE",
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.15.0",
    "katex": "^0.16.45",
    "puppeteer": "^24.0.0"
  }
}
